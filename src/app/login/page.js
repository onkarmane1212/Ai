import LoginForm from '@/components/auth/LoginForm';

export const metadata = {
    title: 'Login - Web Search App',
    description: 'Sign in to your account',
};

export default function LoginPage() {
    return <LoginForm />;
}
