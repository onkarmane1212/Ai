import SignupForm from '@/components/auth/SignupForm';

export const metadata = {
    title: 'Sign Up - Web Search App',
    description: 'Create a new account',
};

export default function SignupPage() {
    return <SignupForm />;
}
